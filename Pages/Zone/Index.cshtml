@page
@model WebApp.Pages.Zone.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Zona[0].NomeZona)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Zona) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeZona)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.NomeZona">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.NomeZona">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.NomeZona">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
